---
# Label: ansible_project_secret
# Title: create the Ansible secret for the project in OpenShift
# Mission: create the Ansible secret for the project in OpenShift
# Milestone: Automate Project Deployment
# Column: Automate Project Deployment

apiVersion: v1
stringData:
  JDBC_URL: "jdbc:postgresql://postgres.{{ POSTGRES_NAMESPACE }}.svc:5432/{{ POSTGRES_DB_NAME }}"
  JDBC_USERNAME: "{{ POSTGRES_DB_USER }}"
  JDBC_PASSWORD: "{{ POSTGRES_DB_PASSWORD }}"
  JDBC_DATABASE: "{{ POSTGRES_DB_NAME }}"
  JDBC_HOST: "{{ POSTGRES_HOST_NAME }}"
  JDBC_PORT: "{{ POSTGRES_PORT }}"
  AUTH_REALM: "{{ AUTH_REALM }}"
  AUTH_CLIENT: "{{ AUTH_CLIENT }}"
  AUTH_SECRET: "{{ AUTH_SECRET }}"
  AUTH_HOST_NAME: "{{ AUTH_HOST_NAME }}"
  AUTH_PORT: "{{ AUTH_PORT }}"
  AUTH_SSL: "{{ AUTH_SSL }}"
  AUTH_URL: "{{ AUTH_URL }}"
  EMAIL_HOST: "{{ EMAIL_HOST_NAME }}"
  EMAIL_PORT: "{{ EMAIL_PORT }}"
  EMAIL_FROM: "{{ EMAIL_FROM }}"
  EMAIL_SSL: "{{ EMAIL_SSL }}"
  EMAIL_AUTH: "{{ EMAIL_AUTH }}"
  EMAIL_USERNAME: "{{ EMAIL_USER }}"
  EMAIL_PASSWORD: "{{ EMAIL_PASSWORD }}"
  QUAYIO_ORG: "{{ QUAYIO_ORG }}"
  MAILING_LIST_URL: "{{ MAILING_LIST_URL }}"
  SITE_PUBLIC_URL: "{{ SITE_PUBLIC_URL }}"
  SITE_POM_GROUP_ID: "{{ SITE_POM_GROUP_ID }}"
  SUMO_EXECUTABLE_PATH: "{{ SUMO_EXECUTABLE_PATH }}"
  PYTHON_EXECUTABLE_PATH: "{{ PYTHON_EXECUTABLE_PATH }}"
  DYNAMIC_PAGE_PATH: "{{ DYNAMIC_PAGE_PATH }}"
  YGGIO_HOST_NAME: "{{ YGGIO_HOST_NAME }}"
  YGGIO_PORT: "{{ YGGIO_PORT }}"
  YGGIO_SSL: "{{ YGGIO_SSL }}"
  YGGIO_API_RELATIVE_URI_IotNode: "{{ YGGIO_API_RELATIVE_URI_IotNode }}"
  YGGIO_AUTH_LOCAL_URI: "{{ YGGIO_AUTH_LOCAL_URI }}"
  YGGIO_USERNAME: "{{ YGGIO_USERNAME }}"
  YGGIO_PASSWORD: "{{ YGGIO_PASSWORD }}"
  GITHUB_ORG: "{{ GITHUB_ORG }}"
  SITE_POWERED_BY_URL: "{{ SITE_POWERED_BY_URL }}"
  SITE_POWERED_BY_NAME: "{{ SITE_POWERED_BY_NAME }}"
  SITE_POWERED_BY_IMAGE_URI: "{{ SITE_POWERED_BY_IMAGE_URI }}"
  FCD_SYNC_PATH: "{{ FCD_SYNC_PATH }}"
  FCD_MAX_RECORD_SIZE: "{{ FCD_MAX_RECORD_SIZE }}"
  READ_BUFFER_SIZE: "{{ READ_BUFFER_SIZE }}"
  API_CHECK_TIMER_MILLIS: "{{ API_CHECK_TIMER_MILLIS }}"
  DEFAULT_MAP_LOCATION: "{{ DEFAULT_MAP_LOCATION }}"
  DEFAULT_MAP_ZOOM: "{{ DEFAULT_MAP_ZOOM }}"
  ZOOKEEPER_MAX_RETRIES: "{{ ZOOKEEPER_MAX_RETRIES }}"
  ZOOKEEPER_BASE_SLEEP_TIME_MILLIS: "{{ ZOOKEEPER_BASE_SLEEP_TIME_MILLIS }}"
  ZOOKEEPER_MAX_SLEEP_MILLIS: "{{ ZOOKEEPER_MAX_SLEEP_MILLIS }}"
  ZOOKEEPER_CONNECTION_TIMEOUT_MILLIS: "{{ ZOOKEEPER_CONNECTION_TIMEOUT_MILLIS }}"
  ZOOKEEPER_SESSION_TIMEOUT_MILLIS: "{{ ZOOKEEPER_SESSION_TIMEOUT_MILLIS }}"
  API_COUNTER_RESUME_TimeStep: "{{ API_COUNTER_RESUME_TimeStep }}"
  API_COUNTER_FETCH_TimeStep: "{{ API_COUNTER_FETCH_TimeStep }}"
  API_COUNTER_RESUME_VehicleStep: "{{ API_COUNTER_RESUME_VehicleStep }}"
  API_COUNTER_FETCH_VehicleStep: "{{ API_COUNTER_FETCH_VehicleStep }}"
  I18N_PATHS: "{{ I18N_PATHS }}"
  DYNAMIC_PAGE_PATHS: "{{ DYNAMIC_PAGE_PATHS }}"
  FIWARE_SSL: "{{ FIWARE_SSL }}"
  FIWARE_HOST_NAME: "{{ FIWARE_HOST_NAME }}"
  FIWARE_PORT: "{{ FIWARE_PORT }}"
  FIWARE_API_URI: "{{ FIWARE_API_URI }}"
kind: Secret
type: Opaque
